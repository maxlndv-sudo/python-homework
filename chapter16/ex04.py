def mul_time(time, number):
    """Умножает время на число (например, для расчета интервалов)."""
    # Используем функции преобразования из Упражнения 3
    seconds = time_to_int(time) * number
    return int_to_time(int(seconds))

def find_average_pace(finish_time, distance):
    """Вычисляет средний темп (время на одну милю/километр).
    finish_time: объект Time (общее время гонки)
    distance: число (дистанция)
    """
    # Разделить время на дистанцию — это то же самое, что умножить на (1 / дистанция)
    return mul_time(finish_time, 1 / distance)

# Функция mul_time:
# Напрямую умножить объект времени на число нельзя
# Поэтому мы используем уже готовый мост из Упражнения 3: переводим время в секунды с помощью time_to_int
# Умножаем полученное целое число секунд на переданный множитель: seconds * number
# Oкругляем результат до целого числа с помощью int(), так как секунд в нашей модели должно быть целое количество.
# Переводим результат обратно в формат времени через int_to_time

# Расчет темпа (find_average_pace)
# Математически, разделить время на дистанцию
# У нас уже есть функция умножения времени на число (mul_time). Поэтому вместо написания новой логики деления, мы просто вызываем mul_time(finish_time, 1 / distance)
