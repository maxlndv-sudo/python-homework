def time_to_int(time):
    """Преобразует объект Time в целое число секунд с начала дня."""
    minutes = time.hour * 60 + time.minute
    seconds = minutes * 60 + time.second
    return seconds

def int_to_time(seconds):
    """Преобразует целое число секунд обратно в объект Time."""
    time = Time()
    minutes, time.second = divmod(seconds, 60)
    time.hour, time.minute = divmod(minutes, 60)
    return time

def add_time(t1, t2):
    """«Чистая» функция сложения двух объектов Time через секунды."""
    seconds = time_to_int(t1) + time_to_int(t2)
    return int_to_time(seconds)

# Перевод времени в секунды (time_to_int):
# В одном часе 60 минут. Чтобы перевести часы в минуты, умножаем их на 60 и прибавляем имеющиеся минуты: minutes = hour * 60 + minute.
# В одной минуте 60 секунд. Чтобы получить общее число секунд, умножаем полученные минуты на 60 и прибавляем секунды: seconds = minutes * 60 + second.

# Сложение:
# Теперь, когда оба объекта времени стали обычными целыми числами (секундами), мы просто складываем их: total_seconds = seconds1 + seconds2. 
# Сложение двух чисел в Python происходит мгновенно и без ошибок.

# Обратный перевод секунд во время (int_to_time):
# Нам нужно сделать обратную операцию, используя деление с остатком. 
# Для этого идеально подходит встроенная функция divmod(a, b), которая возвращает сразу два значения: (результат деления, остаток).
# Шаг 1: Делим общие секунды на 60. minutes, seconds = divmod(total_seconds, 60). 
# Остаток — это наши финальные секунды. А результат деления — это общее количество минут.
# Шаг 2: Полученные минуты делим на 60. hours, minutes = divmod(minutes, 60). 
# Остаток — это финальные минуты, а результат деления — часы.
# Шаг 3: Записываем эти три числа в атрибуты нового объекта класса Time и возвращаем его.
